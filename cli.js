#!/usr/bin/env node

/**
 * Entry point for the `plugs` command.
 * Per the build plan: this file does nothing except hand off to the
 * launcher. All real logic lives in launcher.js.
 */

require("./launcher.js");